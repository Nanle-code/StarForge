# Golden CLI case: network add rejects a reserved network name
description = "network add rejects a reserved network name"
args = ["network", "add", "testnet", "--horizon-url", "https://example.com"]
